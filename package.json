{
  "name": "unischema",
  "version": "0.1.0",
  "description": "Webhook Unifier for university advancement teams",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:security": "vitest run tests/security.test.ts",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:all": "npm test && npm run test --prefix frontend",
    "validate": "npm run typecheck && npm run test:security && npm run test:coverage && npm run typecheck --prefix frontend && npm run test --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [
    "webhook",
    "university",
    "advancement",
    "zod",
    "hono"
  ],
  "license": "MIT",
  "dependencies": {
    "hono": "^4.12.26",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@hono/node-server": "^2.0.5",
    "@types/node": "^26.0.0",
    "@vitest/coverage-v8": "^4.1.9",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
